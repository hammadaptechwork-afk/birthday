name: watchhub
description: Premium Watch E-commerce Application
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter

  # UI Components
  cupertino_icons: ^1.0.6

  # State Management
  provider: ^6.1.1

  # Utilities
  shared_preferences: ^2.2.2
  uuid: ^4.3.0
  intl: ^0.19.0

  # Navigation
  go_router: ^13.0.1

  # Image Loading
  cached_network_image: ^3.3.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0

flutter:
  uses-material-design: true